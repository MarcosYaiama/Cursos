" Execução do gerenciador de plugin
execute pathogen#infect()

" Alterando tecla do Emmet
let g:user_emmet_leader_key=","

" Esquema de cores escuro
colorscheme Tomorrow-Night-Eighties

" Tamanho da indentação
set tabstop=2

" Identifica o tipo de arquivo e indenta
filetype plugin indent on

" Colorir o editor
syntax on

" Deixar coerente indentação com tamanho de TAB 
set shiftwidth=2

" Comportamento usual com backspace
set backspace=2

" Esse comando serve para numerar as linhas
set number

" Fazer calculo da distancia das linhas
set relativenumber

" Busca incremental - feedback enquanto busco
set incsearch

" Destaque no resultados
set hlsearch

" Salvar na codificação desejada
" set fileencoding=iso-8859-1

" Visualizar na codificação desejada
"set encoding=iso8859-1

" Usa espaÃos no lugar de tabs
set expandtab

" backspace respeita identaÃÃo
set softtabstop=2

